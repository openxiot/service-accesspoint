quarkus.http.test-port=8081

product-center-api/mp-rest/url=http://localhost:18080
product-center-api/mp-rest/scope=jakarta.inject.Singleton

# Fast MongoDB failure in tests — no Docker → no MongoDB container
# Without this, MongoDB finds hang for 30s and fail during shutdown
quarkus.mongodb.server-selection-timeout=PT0.5S

# Dummy URL for DeviceEventPublisher REST client (not directly tested)
device-event-api/mp-rest/url=http://localhost:18081
